Activity 1:
1. 
a. public static double sentimentVal(String word)
b. Yes, it requires a String parameter. Passing in a different parameter results in a error: incompatible types
c. Yes, it returns a double

2.
sentimentVal("word"); -> 0.11
sentimentVal("thing"); -> -0.76
sentimentVal("person"); -> -0.14

3. 
a. It would compile, because the variable type (double) matches the return method type (double)
b. It will not compile, because the parameter 0.5 does not match the String one the method requires
c. It will not compile, because sentimentVal only takes one String as a parameter

Activity 2:
1. 
